### Helpers I usually use

- [xdebug on/off](https://github.com/w00fz/xdebug-osx)

### Load bash aliases

- `aliases.zsh` files are usually loaded using `source /path/to/dotfiles/phpstan/aliases.zsh` from within `~/.zshrc`
